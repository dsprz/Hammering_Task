import pinocchio
# model_path = "/path/to/your/urdf_folder"
# urdf_filename = "robot.urdf"
# urdf_path = model_path + "/" + urdf_filename


# model, _, _ = pinocchio.buildModelsFromUrdf(urdf_path, package_dirs=[model_path])
# data = model.createData()

# # Target frame and pose (e.g., end-effector)
# target_frame = "hammer_head"  # replace with your end-effector frame
# target_id = model.getFrameId(target_frame)